{% extends 'base.html.twig' %}

{% block body %}


    {{ parent() }}
    <a href="{{path('universidadmi_homepage')}}"><img class="ima" src="{{ asset('public/imagenes/flecha.png')}}" alt=""> </a>

      {{ form_start(Form) }}
     <div id="simpleform"  class="container-fluid">

                      <label for="">Nombre</label>
                      {{form_widget(Form.nombre)}}
                      <label for="">Apellido</label>
                      {{form_widget(Form.apellido)}}
                      <label for="">cedula</label>
                      {{form_widget(Form.cedula)}}
                      <label for="">Periodo Academico</label>
                      {{form_widget(Form.periodoAcademico)}}
                      <label for="">Fecha de Nacimiento</label>
                      {{form_widget(Form.fechaNacimientoAt)}}
                      <label for="">Foto Estudiante</label>
                      {{form_widget(Form.foto)}}
                      <label for="">Carrera</label>
                      {{form_widget(Form.carrera)}}
                      <br><br>
                      {{form_widget(Form.subir,{'attr': {'class':'btn btn-success'}})}}


                </div>

                {{ form_end(Form) }}




{% endblock %}
