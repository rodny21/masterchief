{% extends 'base.html.twig' %}

{% block body %}

    {{ parent() }}
    <div class="container">
      <h2 class="text-center" style="font: Time-new-roman;">
      Bienvenido
    {{ app.user.username }}
      </h2>
<form class="navbar-form navbar-left" action="{{path('universidadmi_buscar')}}" method="post" name="page" style="margin-left: 80%;">
    <div class="form-group">
<input  class="form-control" type="text" placeholder="Carrera" name="page"></input>
    </div>
    <button type="submit" class="btn btn-default" style="margin-left: 25%;">Submit</button>

</form>
<br><br>


    <div class="caja1" ><a href="{{path('estudiante_nuevo')}}"><img class="im" src="{{ asset('public/imagenes/usuario.png') }}" alt="">Registrar Estudiante</a>
    <div class="caja2">
    <a href="{{path('estudiante_lista')}}"><img class="im" src="{{ asset('public/imagenes/lista.png') }}" alt="">Lista de Estudiante</a>
    </div>  </div>
    <br><br>
    {% if is_granted ('ROLE_PROFESOR') %}
    <div class="caja1"><a href="{{path('profesor_nuevo')}}"><img class="im" src="{{ asset('public/imagenes/cliente.png') }}" alt="">Registrar Profesor</a>
    <div class="caja2">
    <a href="{{path('profesor_lista')}}"><img class="im" src="{{ asset('public/imagenes/lista.png') }}" alt="">Lista de Profesor</a>
    </div></div>
  {% endif %}


{% endblock %}
